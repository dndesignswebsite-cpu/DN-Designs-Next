$kind: http-request
description: Delete specific image from page (Admin only)
url: "{{base_url}}/api/pages/{{page_id}}/images/{{image_id}}"
method: DELETE
headers:
  Authorization: Bearer {{admin_token}}
order: 10000
