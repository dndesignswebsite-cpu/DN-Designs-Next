$kind: http-request
description: Delete image from blog post (Admin only)
url: "{{base_url}}/api/blogs/{{blog_id}}/images/{{image_id}}"
method: DELETE
headers:
  Authorization: Bearer {{admin_token}}
order: 8000
